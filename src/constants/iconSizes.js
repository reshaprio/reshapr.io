/**
 * Pixel sizes for Lucide / BrandSocialIcon `size` props.
 * `--reshapr-icon-size` (16px) is navbar/CSS-only — no shared JS constant yet.
 * Keep in sync with `src/css/custom.css` `--reshapr-icon-size*` tokens.
 */
export const ICON_SIZE_INLINE_PX = 18; // --reshapr-icon-size-inline
export const ICON_SIZE_LP_FEATURE_PX = 32; // --reshapr-icon-size-lp-feature (2rem)
